import React from "react"
import styled from "styled-components"

const Connect = styled.div`
  position: absolute;
  top: 5000px;
`

const Contact = () => {
  return <Connect id="contact">Hello</Connect>
}

export default Contact
